# Repository Guidelines（仓库协作指南）

## 工作原则

### 1. 编码前先思考

- 实现前识别关键假设、歧义、依赖和取舍，不要默默选择会明显改变功能或范围的理解。
- 只有当不确定性会实质影响行为、数据或安全时才询问；低风险细节应基于现有代码作出合理假设并继续。
- 开始修改前先阅读相关代码、配置和已有约定，确认真实原因与影响范围。

### 2. 简单优先

- 使用能够完整解决当前需求的最少代码，不添加未被要求的功能、抽象或配置项。
- 不为单次逻辑过早建立通用框架；只有出现明确复用需求时才抽取公共能力。
- 简单不等于忽略异常，应处理与当前功能直接相关的失败、空状态、重复操作和边界条件。

### 3. 精准修改

- 只修改完成当前需求必须触及的代码，不顺手重构、格式化或删除无关内容。
- 遵循现有架构和代码风格，保留用户已有改动，不覆盖不属于当前任务的差异。
- 清理本次修改造成的失效导入、变量和分支；对原本存在的无关问题只做说明。

### 4. 目标驱动与可验证交付

- 开始前明确成功标准；复杂任务拆成可验证的小步骤，直到全部标准满足。
- 修复缺陷时先确认复现条件和根因；重构时保证修改前后外部行为一致。
- 验证强度与风险匹配：文档检查格式，逻辑运行检查和构建，关键交互进行对应环境验证。

## 通用执行流程

1. **理解任务**：确认目标、范围、限制和验收结果。
2. **检查现状**：读取相关文件、配置、命令和工作区状态，不凭假设修改。
3. **实施变更**：保持改动集中、小而完整，并沿用仓库已有实现方式。
4. **执行验证**：优先运行与改动最相关的检查；若无法执行，明确说明原因和未验证风险。
5. **交付说明**：概述完成内容、关键文件、验证结果及仍需用户处理的事项。

## 通用变更与安全边界

- 不提交密钥、Token、账号、私有配置或包含真实用户数据的文件。
- 未经明确要求，不切换环境、修改外部服务地址、升级依赖或执行破坏性数据操作。
- 不覆盖无关的未提交改动；发现冲突时先缩小修改范围，无法安全继续时再说明阻塞。
- 新增依赖前先确认现有能力无法满足，并说明引入原因、体积和维护影响。

## 通用提交与交付规范

提交信息优先使用 Conventional Commits，例如 `feat(profile): add avatar upload`、`fix(auth): guard protected routes`。Pull Request 应说明用户可见变化、配置或环境影响、验证结果和已知限制。除非用户明确要求，不主动提交、推送或发布代码。

## 项目专属约定：爱恋存档

### 项目概览

本仓库是“爱恋存档”微信小程序前端，使用 uni-app、Vue 3、TypeScript、Pinia 和 SCSS。产品应保持温暖、治愈的视觉风格。

### 项目结构

- `src/pages/`：页面；首页路由固定为 `pages/index/index`。
- `src/components/`：通用组件，包括卡片、图标、分段控件和 loading。
- `src/api/`：类型化接口与响应转换；请求统一经过 `src/api/request.ts`。
- `src/stores/`：Pinia 状态，管理登录、绑定关系、邀请码和心情缓存。
- `src/types/`：共享领域类型；`src/static/` 存放本地静态资源。
- `design-system/`：项目的视觉方向与设计约束。

### 开发与验证命令

- `npm install`：按锁定版本安装依赖。
- `npm run dev:mp-weixin`：监听编译微信小程序。
- `npm run dev:h5`：运行 H5 开发版。
- `npm run type-check`：检查 Vue 与 TypeScript 类型。
- `npm run build:mp-weixin`：生成 `dist/build/mp-weixin` 上传包。

交付代码前必须运行类型检查和小程序构建。当前没有完整的前端自动化测试；涉及交互、登录、授权或路由时，还需在微信开发者工具中验证。

### 编码与 UI 约定

使用 Vue Composition API 和 `<script setup lang="ts">`，两个空格缩进，TypeScript 使用单引号，页面样式使用 scoped SCSS。组件文件使用 PascalCase，变量和函数使用 camelCase。接口数据转换放在 `src/api/`，不要散落在页面模板中。优先复用 `AppIcon`、`SegmentControl`、`LoadingIndicator` 等已有组件。

列表使用稳定的 `:key="record.id"` 和分页缓存。刷新时保留已有内容，优先局部 loading，避免整页闪烁。保持现有温暖配色、间距和图标体系，不引入风格不一致的组件。

微信小程序中的轻量图文点击入口使用 `view + @tap + hover-class` 实现，不要用原生 `button` 包裹图标和文字，以免默认行高、内边距影响布局。仅在表单提交、微信授权、头像选择等需要原生按钮能力时使用 `button`。

### 产品与数据底线

- 未经用户明确要求，不得修改 `VITE_API_BASE_URL` 或切换本地、测试、正式环境。
- 心情发布后不可编辑或删除，前端不得暴露心情删除入口。
- 用户可编辑自己的评论，不得修改他人评论。
- “仅自己可见”的心情始终仅归本人。对象只能看到本次绑定开始后发布且对其可见的记录。
- 解除绑定后双方立即失去对彼此记录和统计的访问权限。
- 受保护页面必须在跳转前检查登录状态，避免先显示目标页再闪到登录页。
